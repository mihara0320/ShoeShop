from __future__ import unicode_literals
from django.db import models
from datetime import datetime

# class Cart(models.Model):
#     items = models.CharField(max_length = 50, default='Empty')
#     total_price = models.CharField(max_length = 50, default='Empty')
    #
    # def __str__(self):
    #     return self.items
